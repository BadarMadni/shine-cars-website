import LoginContent from "@/components/pages/LoginContent";

export const metadata = {
  title: "Login | Shine Cars",
  description: "Log in to your Shine Cars account to book rides.",
};

export default function LoginPage() {
  return <LoginContent />;
}
